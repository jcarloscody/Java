MAPEAMENTO OBJETO-RELACIONAL COM JPA/HIBERNATE - JAVA EE - ENTERPRASE EDTION


JAVA EE - conjunto de especificacoes e uma das mais importante delas é a JPA.

JPA - é a biblioteca padrao de persistencia de dados no java baseado no principio
do mapeamento objeto-relacional. JPA é a especificacao dessa bibblioteca.

HIBERNATE: é uma das implementacoes da especificacao JPA. e o Hibernate é uma das implementacoes
mais popular de todas. 

JPA - especificacao, interface
HIBERNATE - implementacao


Ferramentas:
Spring Tools: é do eclipse e vem com os plugins para trabalhar com o spring boot
Postman

--------------------------------------------------------
O banco de dados não é orientado a objeto, mas é construido sob o paradigma relacional,
entao quando tinhamos que fazer um esforço enorme para emparelhar o nosso sistema
orientado a objeto com o bd relacional.

ENTÃO o mapeamento obj-rel veio para isso, facilitar a comunicacao entre o sistema orientado a 
obj com o bd relacional 

-----------------------------------

JPA - Java Persistence API
-> é a especificacao padrao da plataforma Java EE (pacote javax.persistence) para mapeamento 
objeto-relacional e persistencia de dados

- é apenas uma especificacao (jsr 338)

-> para trabalhar com JPA é preciso incluir uma implementacao da API -> a HIBERNATE

-> no JPA temos os Arquivos de Configuracao e o Mapeamento

-> Mapeamento: Qual classe corresponde a qual tabela, dentro da classe quais atribubtos correspondem
aos campos da tabela, e tbm os relacionamentos


----------------------------------------------------
PRINCIPAIS CLASS
EntityManager ->
- instancia uma encapsulacao para conectar ao bd
- faz operacoes de acesso a dados(crud) 
- Um objeto EntityManager encapsula uma conexão com a base de dados e serve para efetuar operações de
acesso a dados (inserção, remoção, deleção, atualização) em entidades (clientes, produtos, pedidos, etc.)
por ele monitoradas em um mesmo contexto de persistência.

- o que é obj monitorado? é o obj que vc acabou de inserir ou é o obj que vc buscou no bd e ainda nao 
fechou o entitiemaneger 

Escopo: tipicamente mantem-se uma instância única de EntityManager para cada thread do sistema (no caso
de aplicações web, para cada requisição ao sistema).
- ou seja, quando o user esta usando uma aplicacao webb e faz uma requisicao, essa aplicacao vai instanciar
um unico entitymanager para trabalhar com todos os dados daquela requisicao.




EntityManagerFactory ->>
https://docs.oracle.com/javaee/7/api/javax/persistence/EntityManagerFactory.html

 - Um objeto EntityManagerFactory é utilizado para instanciar objetos EntityManager.
 Escopo: tipicamente mantem-se uma instância única de EntityManagerFactory para toda aplicação.
 
 
 
 
 
 --------------------------------------------------
 Maven - gerenciador de dependencias e building do java
 groupid -> costuma ser o nome do pacote da sua empresa
 artifact id -> nome do projeto
 
 -----------------------------------------
-> arquivo pom.xml: aqui vai estar as configuracoes maven, quais dependencias ele tem que baixar
, qual a versao do java ele tem que compilar, como ele vai fazer o build da aplicacao.

-> arquivo persistence.xml: arquivo que contem a configuracao do JPA
 
 
 -----------------------------------